/** The five valid choice names as a union — catches typos at compile time. */
export type ChoiceName = "rock" | "paper" | "scissors" | "lizard" | "spock";

export type GameResult = "win" | "lose" | "tie";


